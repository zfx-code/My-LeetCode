"""
腾讯2024.3.30笔试


所有的无限循环小数都可以写成分数形式
判断循环节长度为k的无限循环小数的分母是否可能是p

即a是分子, a/p为循环节长度为k的无限循环小数

结论: 若有p的因子i使得10^k-1能整除i, 则存在a/p
否则不存在

i | 10^k - 1
=> 10^k % i = 1

p的质因子只含[2, 5] => 有限小数
否则 i | 10^k - 1, 存在分子
否则不存在
"""

N = int(pow(10 ** 9, 0.5)) + 5

k = 3
p = 37 

def check_factor(k, p):
    while p % 2 == 0:
        p /= 2 
    
    while p % 5 == 0:
        p /= 5 
    
    # 有限小数
    if p == 1:
        return False

    return (pow(10, k, p) - 1) % p == 0
